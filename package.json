{
  "name": "nimble",
  "private": true,
  "scripts": {
    "test": "node --test 'test/*.test.*js'"
  },
  "description": "Ask a question, get one sentence back. Math on-device, facts from open models."
}
